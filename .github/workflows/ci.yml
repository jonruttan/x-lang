name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: make test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14, ubuntu-24.04]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Build
        run: make
      # The contract gates (`make test` runs them first): C-surface ratchet
      # + object-layout + base-paths.  Without this step the ratchet only
      # fires on machines with the local pre-push hook.
      - name: Contract gates
        run: make check-isa check-obj-layout check-base-paths
      - name: C specs
        run: make test-c
      - name: x-lang specs
        run: make test-x
        env:
          PARALLEL: "1"

  # Memory-safety gate: both suites under AddressSanitizer. HARD gate since
  # the baseline reached zero (2026-07-13; was 112 findings): a red run here
  # is a real memory-safety regression -- the class that is silently wrong
  # on 64-bit and segfaults on 32-bit/Pi. See test-asan in the Makefile for
  # the pinned ASAN_OPTIONS rationale.
  asan:
    name: make test-asan
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Run suites under AddressSanitizer
        run: make test-asan
        env:
          PARALLEL: "1"
