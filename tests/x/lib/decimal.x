; Test harness: x-core.x + bigint + float + rational + complex + decimal
; (the whole real line, so the cross-type cases have something to cross)
(include "lib/x-core.x")
(import x/num/bigint)
(import x/num/float)
(import x/num/rational)
(import x/num/complex)
(import x/num/decimal)
