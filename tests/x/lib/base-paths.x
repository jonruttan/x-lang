; Test harness: x-core.x only -- the base-paths descriptor and the reflect
; walker are already part of boot, so specs exercise the LIVE %base-paths
; and %reflect-* definitions directly.
(include "lib/x-core.x")
